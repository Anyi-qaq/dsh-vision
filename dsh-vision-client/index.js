// dsh-vision-client — host half (intentionally empty).
// This package exists to ship the browser settings UI through the `dsh.client`
// table. All capabilities live in the dsh-vision plugin (host tools) and in
// lib/client.js (browser settings sections).
export function apply() {}
